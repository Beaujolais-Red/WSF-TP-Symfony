<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">


        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
    {% block stylesheets %}

        {#{{ encore_entry_link_tags('app') }}#}
    {% endblock %}

    {% block javascripts %}
      <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
    {% endblock %}
</head>


<body style=" display: flex;
        min-height: 100vh;
        flex-direction: column;">

  {# header #}

  <nav class="grey darken-1">
    <div class="nav-wrapper">
      <a href="#" class="brand-logo">
      <img src="https://zupimages.net/up/21/50/rz3y.png" />
      </a>
      <a href="#" data-target="mobile-demo" class="sidenav-trigger"><i class="material-icons">menu</i></a>
      <ul class="right hide-on-med-and-down">
        <li><a href="{{ path('games_index') }}">Jeux</a></li>
        <li><a href="{{ path('console_index') }}">Consoles</a></li>
        <li><a href="{{ path('developer_index') }}">Développeurs </a></li>
        <li><a href="{{ path('genre_index') }}">Genre </a></li>
        <li><a href="{{ path('app_register') }}"><span class="new badge pink darken-4" data-badge-caption="S'enregistrer"></span></a></li>
        <li><a href="{{ path('app_login') }}"><span class="new badge pink darken-4" data-badge-caption="S'identifier"></span></a></li>
      </ul>
    </div>
  </nav>

  <ul class="sidenav" id="mobile-demo">
    <li><a href="{{ path('games_index') }}">Jeux</a></li>
    <li><a href="{{ path('console_index') }}">Consoles</a></li>
    <li><a href="{{ path('developer_index') }}">Développeurs </a></li>
    <li><a href="{{ path('genre_index') }}">Genre </a></li>
  </ul>
        

    <div class="container">
        {% block body %}{% endblock %}
    </div>

    // <script>
    //     {% for type, messages in app.flashes() %}
    //         {% for message in messages %}
    //             M.toast({html: '{{ message }}', classes: '{{ type }}'});
    //         {% endfor %}
    //     {% endfor %}

    //     document.addEventListener('DOMContentLoaded', function() {
    //         var elems = document.querySelectorAll('select');
    //         var instances = M.FormSelect.init(elems);
    //     });
    // </script>
            

</body>
</html>
